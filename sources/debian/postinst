#!/bin/bash
set -e

# Make the application executable
chmod +x /opt/oro-avalonia/Oro

# Update desktop database if available
if command -v update-desktop-database > /dev/null 2>&1; then
    update-desktop-database /usr/share/applications
fi

exit 0
